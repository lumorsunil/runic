# lib.rn

const name = printf "lib"
const surname = ".rn"
const fullname = printf "${name.stdout} ${surname}"
const again = printf "${name}"

echo "hello from ${fullname}"
echo "hello again ${again}"

fn hello() Void {
  const name = printf "lib"
  const surname = ".rn"
  const fullname = printf "${name.stdout} ${surname}"
  echo "hello from ${fullname}"
  echo "hello from ${fullname}"
}
