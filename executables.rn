# executables.rn

const echo = &!echo
const !echo, !cat

cat "file.file" | cat

fn Void add(x: Int, y: Int) Int {
  return x + y
}

const add5 = &add 5

const echoWithGreeting = &echo "hello"
echoWithGreeting ", world!"

