fn Void source() String {
    yield "stage"
}

fn String middle() String {
    const s = @stdin
    yield "three ${s}"
}

fn String sink() String {
    const s = @stdin
    yield "${s} pipeline"
}

source | middle | sink
