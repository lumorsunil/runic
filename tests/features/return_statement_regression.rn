fn Void classify(value: Int) String {
  if (value < 0) {
    yield "negative"
    return
  }

  if (value == 0) {
    yield "zero"
    return
  }

  yield "positive"
}

echo "${classify(1)}"
echo "${classify(0)}"
echo "${classify(3)}"
