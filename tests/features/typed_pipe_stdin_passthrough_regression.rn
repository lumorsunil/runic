// An explicit passthrough stage `{ yield &0 }` re-emits its input unchanged,
// preserving its type. Here the Int from parseInt flows through the passthrough
// into doubler.

fn Int doubler() Int {
    yield &0 * 2
}

echo "5" | parseInt | { yield &0 } | doubler
