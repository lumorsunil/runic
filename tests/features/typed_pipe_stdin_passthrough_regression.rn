// An explicit passthrough stage `{ yield @stdin }` re-emits its input unchanged,
// preserving its type. Here the Int from parseInt flows through the passthrough
// into doubler.

fn Int doubler() Int {
    yield @stdin * 2
}

echo "5" | parseInt | { yield @stdin } | doubler
