# cmd/runic/

This directory houses the Runic CLI entry point. Future commits should place
the main executable sources here so end users can build `cmd/runic` and run the
interpreter or REPL with consistent tooling.
